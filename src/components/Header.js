import React from 'react';
import { AppBar, Toolbar } from '@material-ui/core';

const Header = props => {
    return (
        <AppBar>
            <Toolbar>
                <Typography>
                    
                </Typography>
            </Toolbar>
        </AppBar>
    )
};

export default Header;