import React from 'react';
import { Typography, Grid } from '@material-ui/core';

const Shop = props => {
    return (
        <Grid container>
            <Grid item>
                <Typography>
                    Shop
                </Typography>
            </Grid>
        </Grid>
    );
};

export default Shop;