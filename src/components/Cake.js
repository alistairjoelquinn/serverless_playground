import React from 'react';
import { Typography } from '@material-ui/core';

const Cake = props => {
    return (
        <Typography>
            Cake
        </Typography>
    );
};

export default Cake;